import PutioAPIClient from '../client';
export default class Tunnel {
    private client;
    constructor(client: PutioAPIClient);
    Routes(): Promise<import("..").IPutioAPIClientResponse<any>>;
}
