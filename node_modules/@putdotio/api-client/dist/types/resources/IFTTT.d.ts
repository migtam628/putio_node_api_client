import PutioAPIClient from '../client';
export default class IFTTT {
    private client;
    constructor(client: PutioAPIClient);
    SendEvent({ clientName, eventType, ingredients, }: {
        clientName?: string;
        eventType: string;
        ingredients: object;
    }): Promise<import("..").IPutioAPIClientResponse<any>>;
}
