import PutioAPIClient from '../client';
export default class Trash {
    private client;
    constructor(client: PutioAPIClient);
    List({ limit }?: {
        limit?: number;
    }): Promise<import("..").IPutioAPIClientResponse<any>>;
    Continue(cursor: string, { limit }?: {
        limit?: number;
    }): Promise<import("..").IPutioAPIClientResponse<any>>;
    Restore({ useCursor, ids, cursor, }: {
        useCursor?: boolean;
        ids?: number[];
        cursor?: string;
    }): Promise<import("..").IPutioAPIClientResponse<any>>;
    Delete({ useCursor, ids, cursor, }: {
        useCursor?: boolean;
        ids?: number[];
        cursor?: string;
    }): Promise<import("..").IPutioAPIClientResponse<any>>;
    Empty(): Promise<import("..").IPutioAPIClientResponse<any>>;
}
